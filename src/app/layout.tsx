import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "InstaScraper Pro - Instagram Analytics & Growth Tools",
  description: "Discover trending audios, viral hashtags, and viral posts. Get the roadmap to Instagram success with our professional analytics platform.",
  keywords: "Instagram analytics, trending hashtags, viral posts, Instagram growth, social media tools",
  authors: [{ name: "InstaScraper Pro" }],
};

export const viewport = {
  width: "device-width",
  initialScale: 1,
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
